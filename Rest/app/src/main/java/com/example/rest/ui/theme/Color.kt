package com.example.rest.ui.theme

import androidx.compose.ui.graphics.Color

// Colores Base
val Negro = Color(0xFF000000)
val Blanco = Color(0xFFFFFFFF)
val Transparente = Color(0x00000000)

// Colores Principales (Primary)
val Primario = Color(0xFF00B8D4)

// Colores de Acento (Accent)
val Acento = Color(0xFFFF4081)
val AcentoClaro = Color(0xFFFF80AB)

// Colores de Fondo (Background)
val FondoPrimario = Color(0xFFFFFFFF)
val FondoSecundario = Color(0xFFF0F0F0)
val Fondo = FondoPrimario

// Colores de Texto (Text)
val TextoPrimario = Color(0xFF000000)
val TextoSecundario = Color(0xFF212121)

// Colores de Bordes (Borders)
val Borde = Color(0xFF000000)

// Colores de Botones (Buttons)
val BotonDeshabilitado = Color(0xFFE0E0E0)
val BotonTextoDeshabilitado = Color(0xFF9E9E9E)

// Colores de Campos de Entrada (Text Edit)
val EditTextFondo = Color(0xFFFFFFFF)
val InputBordeEnfocado = Color(0xFF00BCD4)
val InputTexto = Color(0xFF000000)

// Colores de Estado (Status)
val EstadoExito = Color(0xFF4CAF50)
val EstadoError = Color(0xFFF44336)
val EstadoAdvertencia = Color(0xFFFF9800)
val EstadoInformacion = Color(0xFF2196F3)

// Colores Específicos de la App
val AzulRestCycle = Color(0xFF00BCD4)
val CyanRest = Color(0xFF00BCD4)
